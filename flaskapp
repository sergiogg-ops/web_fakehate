# /etc/nginx/sites-available/flaskapp
server {
    listen 80;
    server_name snell.prhlt.upv.es;

    location / {
        include proxy_params;
        proxy_pass http://unix:web_fakehate/flaskapp.sock;
    }
}